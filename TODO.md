# DoTalk TODO

未做 / 待办，按主题记着，做的时候删掉对应条目。

## 复习 & 记忆强化
- [ ] **复习 v2：开口说 + 判定**。现在每日复习是"自评（记得/不记得）"。升级成：看中文提示 → 用麦克风开口说英文 → STT/LLM 判定有没有用对目标表达 → 用客观结果驱动 Leitner 升降级，替代自评。（要麦克风闭环，得真人测。）
- [ ] **训练交互层**（drills 的 layer 2）：表达卡片开口造句判定、重答同一题录音、跟读模范答案打分。
- [ ] **长期弱点档案**：跨多场聚合反复出现的问题（比如老说 "which which"、老缺某类词），生成"你的弱点清单 / 生词本"，持续追踪进步曲线。

## 账号 & 持久化
- [ ] **账号 + 服务端存储**：现在历史、JD、复习牌库全在 localStorage —— 单浏览器、不能跨设备。
- [ ] **每日复习提醒（推送/邮件）**：需要账号 + 服务端定时任务，localStorage 做不了。

## 语音体验
- [ ] **STT 收尾慢的悬案**：长答案说完到确认结束偏慢（eot_timeout）。加客户端计时探针定位是"转写慢"还是"eot 收尾等"，再决定要不要上 eager_eot 或"手动结束"控件。
- [ ] **口音客观评分**：现在是 Gemini 从音频给的定性点评，不是逐音素校准分数。要严格评分得上专门的发音评估（如 Azure Pronunciation Assessment）。
- [ ] **句内实时流式 TTS**：之前为修播放卡死，从 MediaSource 退回了整句 blob 播放，每句多等约 1s。想更快可重做 MediaSource（需真人验证音频）。

## 上线前
- [ ] **接口安全**：`/api/deepgram-token` 现在公开且 TTL 1 小时、`/api/tts` `/api/turn` `/api/observe` 全裸奔。上线前收紧 token TTL + 加速率限制 / 简单 session 校验，否则是烧钱漏洞。
- [ ] **用户验证**：拉几个真实目标用户用一轮，验证"职场协作英语 + 诊断→训练"这个闭环是不是真需求。
